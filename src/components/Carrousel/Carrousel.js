import React from "react";
import "./Carrousel.css";

const Carrousel = (props) => {
	const gallery = [];

	// props.items.array.forEach((element) => {
	// 	gallery.push(element.images.web.url);
	// });

	return <div className="carrousel"></div>;
};

export default Carrousel;
